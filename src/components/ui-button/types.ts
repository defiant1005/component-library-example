export enum ButtonColors {
  ACCENT = "accent",
  PRIMARY = "primary",
  SECONDARY = "secondary",
  NEGATIVE = "negative",
  POSITIVE = "positive",
}

export enum ButtonSizes {
  SMALL = "s",
  MEDIUM = "m",
  LARGE = "l",
}

export enum ButtonTypes {
  BUTTON = "button",
  RESET = "reset",
  SUBMIT = "submit",
}

export enum Targets {
  BLANK = "_blank",
  SELF = "_self",
  PARENT = "_parent",
  TOP = "_top",
}
